"use client";

// Session 3: Full login form with Supabase Auth + staff check will be implemented here.
// Placeholder renders a minimal shell so the route resolves during Session 1.

export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-zinc-50 p-4">
      <div className="w-full max-w-sm rounded-2xl bg-white p-8 shadow-sm">
        <h1 className="mb-6 text-2xl font-semibold text-zinc-900">ShopKeeper</h1>
        <p className="text-sm text-zinc-500">Login form coming in Session 3.</p>
      </div>
    </main>
  );
}
